# Ollama configuration (all optional -- defaults shown)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# Email sync configuration (optional -- can also be set in the UI)
# For Gmail: use an App Password from https://myaccount.google.com/apppasswords
# EMAIL_IMAP_HOST=imap.gmail.com
# EMAIL_IMAP_PORT=993
# EMAIL_ADDRESS=you@gmail.com
# EMAIL_PASSWORD=your-app-password

# Push notifications to iPhone via ntfy.sh (free)
# 1. Install ntfy app on iPhone: https://apps.apple.com/app/ntfy/id1625396347
# 2. Subscribe to a private topic in the app (e.g. "prajjwal-expenses-xyz123")
# 3. Set the same topic here:
# NTFY_TOPIC=prajjwal-expenses-xyz123
# NTFY_SERVER=https://ntfy.sh
